<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.calculaVirusApp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ChecklistBuyDateActivity"></activity>
        <activity android:name=".CrearInsumoActivity" />
        <activity android:name=".InsumoDetailActivity" />
        <activity android:name=".InsumoActivity" />
        <activity android:name=".ChecklistDetailActivity" />
        <activity android:name=".ChecklistActivity" />
        <activity android:name=".HomeActivity">
        </activity>
        <activity android:name=".LogInGoogle">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>